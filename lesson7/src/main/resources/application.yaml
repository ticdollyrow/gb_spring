server:
  port: 8189
  servlet:
    context-path: /app
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./mydb;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    show-sql: true
    properties:
      hibertnate:
        dialect: org.hibernate.dialect.H2Dialec
  h2:
    console:
      enabled: true
      path: /h2

